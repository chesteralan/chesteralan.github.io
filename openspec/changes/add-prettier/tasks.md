# Tasks: add-prettier

## Implementation

- [x] Install prettier, prettier-plugin-tailwindcss as devDependencies
- [x] Create `.prettierrc` at repo root with project formatting defaults
- [x] Add `format` and `format:check` scripts to `package.json`
- [x] Run `yarn format` to format all source files
- [x] Run `yarn format:check` to verify formatting passes
