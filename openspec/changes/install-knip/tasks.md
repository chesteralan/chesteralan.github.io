# Tasks: Install Knip

- [x] Install knip as a devDependency (`yarn add -D knip`)
- [x] Create `knip.json` at the project root with entry points and ignore patterns
- [x] Add `check:knip` script to `package.json`
- [x] Run initial `yarn check:knip` and review output
- [x] Address findings — removed unused `firebase` dead code
