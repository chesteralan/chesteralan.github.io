# Tasks: Install Fallow

- [x] Install fallow as a devDependency (`yarn add -D fallow`)
- [x] Create `.fallowrc` at the project root with include/exclude paths and analysis options
- [x] Add `analyze` script to `package.json` (`fallow dead-code && fallow health`)
- [x] Run initial `yarn analyze` and review output
- [x] Address findings — removed unused `framer-motion` dependency
